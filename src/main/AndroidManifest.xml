<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Library_management_system"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AddBooks" />
        <activity android:name=".UpdateBook" />
        <activity android:name=".ViewBook" />
        <activity android:name=".AddMembers" />
        <activity android:name=".UpdateMembers" />
        <activity android:name=".ViewMembers" />
        <activity android:name=".BookLoan" />
        <activity android:name=".ReturnBook" />
        <activity android:name=".BookCopy" />
        <activity android:name=".Publisher" />
        <activity android:name=".BookAuthor" />
        <activity android:name=".Branch" />
    </application>

</manifest>